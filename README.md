This project is an attempt at creating a simple system that allows for a single program to be run with access to all hardware on the raspberry pi.

Things done:
     -GPIO
     -Mailbox
     -FrameBuffer
     -System Timer
     
To Do:
     -USB
     -IRQ source determination and handling
     -eMMC
